# FH6 遥测程序开发参考

> 面向开发 Agent 的需求、协议与算法汇总。  
> 官方资料：[Forza Horizon 6 “Data Out” Documentation](https://support.forza.net/hc/en-us/articles/51744149102611-Forza-Horizon-6-Data-Out-Documentation)  
> 官方文档更新时间：2026-05-15；本参考整理日期：2026-07-22。

## 1. 文档使用约定

本文把信息分为三类，开发时不得混用：

- **官方事实**：FH6 Data Out 文档明确声明的协议、字段、单位或范围。
- **算法推导**：可以由官方字段计算或估计的指标，不是游戏直接输出的数据。
- **待实测假设**：官方没有保证、但实现功能时可能需要依赖的行为，必须通过真实游戏数据验证。

核心结论：FH6 可以输出高频的本车动力学、四轮轮胎和悬架、动力系统、玩家输入、世界坐标及比赛进度，足以实现实时仪表、圈速分析、驾驶指导、调校分析、路线学习、自动路线识别和换挡建议。但它不是完整赛事数据总线，不提供赛道 ID、对手遥测、轮胎磨损或赛道拓扑。

## 2. Data Out 协议约束

以下均为官方事实：

- 使用单向 UDP；游戏只发送，不接收。
- 目标可以是远程 IP，也支持本机 `127.0.0.1`。
- 发包速率等于游戏帧率，不应假定固定为 60 Hz。
- 固定使用一种包格式，包长为 **324 字节**。
- 设置位于 `SETTINGS > HUD AND GAMEPLAY`。
- 应避开端口 `5200` 至 `5300`，游戏会在该范围绑定自己的发送 socket。
- 只在玩家主动驾驶时发送。
- 菜单、暂停、回放、倒带以及比赛结束后不发送。
- `TimestampMS` 是无符号 32 位毫秒时间戳，最终会回绕到 0。

实现要求：

1. 接收器必须接受可变采样率，所有时间计算以 `TimestampMS` 或本地单调时钟为准。
2. UDP 没有可靠传输保证，分析管线必须容忍丢包、重复包和可能的乱序。
3. 数据流停止只代表当前没有驾驶数据，不能单凭停止发包区分暂停、倒带、菜单和比赛结束。
4. 应记录丢包、异常间隔、数据流超时和时间戳回绕，供上层判断单圈有效性。
5. 不要等待一个必然到来的 `IsRaceOn = 0` 作为结束标志；游戏可能直接停止发送。

### 2.1 数值类型

| 标记 | 含义 |
|---|---|
| `S8/S32` | 8/32 位有符号整数 |
| `U8/U16/U32` | 8/16/32 位无符号整数 |
| `F32` | 32 位浮点数 |

### 2.2 包长度与尾部字节

官方声明包长为 324 字节，但文档列出的 88 个命名字段按类型相加为 323 字节。最后 1 字节很可能是结构体对齐产生的填充字节，这是**布局推断，不是官方字段定义**。

解析器应：

- 严格接收 324 字节的 FH6 包；
- 只解析偏移 `0..322` 的命名字段；
- 将偏移 `323` 暂作未定义/填充，不赋予业务含义；
- 对长度不符的数据包单独统计并拒绝或进入兼容解析流程。

官方文档没有明确写出字节序。实现可以先以目标平台常见字节序解析，但必须用 `IsRaceOn`、RPM、速度等合理值进行样本校验，并把结论记录为实测结果。

## 3. 完整字段布局

四轮字段统一使用以下缩写：

- `FL`：左前 Front Left
- `FR`：右前 Front Right
- `RL`：左后 Rear Left
- `RR`：右后 Rear Right

| 字节偏移 | 类型 | 字段 | 官方含义/范围 |
|---:|---|---|---|
| 0 | `S32` | `IsRaceOn` | 驾驶/比赛开启为 1，菜单或比赛停止为 0 |
| 4 | `U32` | `TimestampMS` | 毫秒时间戳；可能回绕 |
| 8 | `F32` | `EngineMaxRpm` | 发动机最大 RPM |
| 12 | `F32` | `EngineIdleRpm` | 发动机怠速 RPM |
| 16 | `F32` | `CurrentEngineRpm` | 当前发动机 RPM |
| 20/24/28 | `F32 × 3` | `AccelerationX/Y/Z` | 车辆局部坐标三轴加速度 |
| 32/36/40 | `F32 × 3` | `VelocityX/Y/Z` | 车辆局部坐标三轴速度 |
| 44/48/52 | `F32 × 3` | `AngularVelocityX/Y/Z` | 局部角速度，rad/s；依次为俯仰、偏航、侧倾 |
| 56/60/64 | `F32 × 3` | `Yaw/Pitch/Roll` | 车辆姿态，弧度 |
| 68/72/76/80 | `F32 × 4` | `NormalizedSuspensionTravelFL/FR/RL/RR` | 归一化悬架行程；0 为最大伸展，1 为最大压缩 |
| 84/88/92/96 | `F32 × 4` | `TireSlipRatioFL/FR/RL/RR` | 归一化纵向滑移率；0 表示充分抓地，绝对值大于 1 表示失去抓地 |
| 100/104/108/112 | `F32 × 4` | `WheelRotationSpeedFL/FR/RL/RR` | 车轮角速度，rad/s |
| 116/120/124/128 | `S32 × 4` | `WheelOnRumbleStripFL/FR/RL/RR` | 对应车轮是否位于路肩，1/0 |
| 132/136/140/144 | `S32 × 4` | `WheelInPuddleFL/FR/RL/RR` | 对应车轮是否位于积水，1/0 |
| 148/152/156/160 | `F32 × 4` | `SurfaceRumbleFL/FR/RL/RR` | 传递给控制器力反馈的无量纲路面震动值 |
| 164/168/172/176 | `F32 × 4` | `TireSlipAngleFL/FR/RL/RR` | 归一化侧向滑移角；0 表示充分抓地，绝对值大于 1 表示失去抓地 |
| 180/184/188/192 | `F32 × 4` | `TireCombinedSlipFL/FR/RL/RR` | 归一化综合滑移；0 表示充分抓地，绝对值大于 1 表示失去抓地 |
| 196/200/204/208 | `F32 × 4` | `SuspensionTravelMetersFL/FR/RL/RR` | 实际悬架行程，米 |
| 212 | `S32` | `CarOrdinal` | 车型唯一编号 |
| 216 | `S32` | `CarClass` | 车辆等级 0 至 7；官方说明 0 为 D、7 为 X |
| 220 | `S32` | `CarPerformanceIndex` | PI，100 至 999 |
| 224 | `S32` | `DrivetrainType` | 0 前驱、1 后驱、2 四驱 |
| 228 | `S32` | `NumCylinders` | 发动机气缸数 |
| 232 | `U32` | `CarGroup` | 车辆组编号 |
| 236 | `F32` | `SmashableVelDiff` | 撞击可破坏物体造成的速度损失，m/s |
| 240 | `F32` | `SmashableMass` | 最近撞到的可破坏物体质量，kg |
| 244/248/252 | `F32 × 3` | `PositionX/Y/Z` | 世界坐标，米 |
| 256 | `F32` | `Speed` | 总速度，m/s |
| 260 | `F32` | `Power` | 功率，W |
| 264 | `F32` | `Torque` | 扭矩，N·m |
| 268/272/276/280 | `F32 × 4` | `TireTempFL/FR/RL/RR` | 四轮轮胎温度；官方未注明单位 |
| 284 | `F32` | `Boost` | 涡轮/机械增压相对大气压的增压值，PSI |
| 288 | `F32` | `Fuel` | 归一化油量；0 空、1 满 |
| 292 | `F32` | `DistanceTraveled` | 累计行驶距离，米 |
| 296 | `F32` | `BestLap` | 最佳圈时间，秒；不适用时为 0 |
| 300 | `F32` | `LastLap` | 上一圈时间，秒；不适用时为 0 |
| 304 | `F32` | `CurrentLap` | 当前圈时间，秒；不适用时为 0 |
| 308 | `F32` | `CurrentRaceTime` | 开始驾驶后的总时间，秒 |
| 312 | `U16` | `LapNumber` | 已完成圈数 |
| 314 | `U8` | `RacePosition` | 当前比赛名次 |
| 315 | `U8` | `Accel` | 油门输入，0 至 255 |
| 316 | `U8` | `Brake` | 刹车输入，0 至 255 |
| 317 | `U8` | `Clutch` | 离合器输入，0 至 255 |
| 318 | `U8` | `HandBrake` | 手刹输入，0 至 255 |
| 319 | `U8` | `Gear` | 当前挡位；官方未说明空挡、倒挡等特殊值映射 |
| 320 | `S8` | `Steer` | 方向输入；-127 全左、0 居中、127 全右 |
| 321 | `S8` | `NormalizedDrivingLine` | 归一化驾驶线位置，-127 至 127 |
| 322 | `S8` | `NormalizedAIBrakeDifference` | 归一化 AI 刹车差值，-127 至 127 |
| 323 | 未定义 | 包尾 | 推测为对齐填充；不得作为业务字段使用 |

车辆局部坐标轴：`X` 向右、`Y` 向上、`Z` 向前。

### 3.1 文档未明确的语义

以下内容不得在没有实测的情况下写死：

- `TireTemp*` 的温标和单位；
- `Acceleration*` 与局部 `Velocity*` 的具体单位；
- 空挡、倒挡和特殊挡位对应的 `Gear` 数值；
- `NormalizedDrivingLine` 正负方向和几何含义；
- `NormalizedAIBrakeDifference` 的精确定义和正负语义；
- `CarOrdinal`、`CarGroup` 到可读车辆名称的官方映射；
- 世界坐标是否被官方保证跨版本永久稳定；
- 包的官方字节序和偏移 323 的用途。

## 4. 可以直接展示与换算的指标

| 指标 | 计算 |
|---|---|
| 速度 km/h | `Speed × 3.6` |
| 速度 mph | `Speed × 2.236936` |
| 功率 kW | `Power / 1000` |
| 功率 hp（机械马力近似） | `Power / 745.699872` |
| 油量百分比 | `Fuel × 100%` |
| 油门/刹车/离合/手刹百分比 | `RawInput / 255 × 100%` |
| 归一化悬架压缩百分比 | `NormalizedSuspensionTravel × 100%` |
| 姿态角度 | `Radians × 180 / π` |

不要在官方未说明加速度单位时直接把 `Acceleration* / 9.80665` 标注为 G。可以提供实验性显示，但必须带单位未确认说明。

## 5. 可以推导的驾驶与车辆分析

### 5.1 驾驶操作

结合位置、速度、时间和输入曲线，可以分析：

- 每个弯的刹车点、松刹点、给油点和全油门点；
- 拖刹入弯、油刹重叠和滑行区间；
- 方向输入速度、修正次数和稳定性；
- 入弯速度、最低弯速和出弯速度；
- 挡位使用、换挡转速、换挡后转速和动力中断；
- 相同弯道在多圈之间的一致性；
- 时间损失发生在制动、弯心还是出弯阶段。

### 5.2 轮胎与抓地

- `TireSlipRatio` 主要用于纵向制动/驱动滑移分析。
- `TireSlipAngle` 主要用于侧向过弯滑移分析。
- `TireCombinedSlip` 用于分析轮胎同时承担制动/驱动和转向时的综合抓地状态。
- 大油门配合驱动轮高滑移，可识别加速空转。
- 大刹车配合某轮高滑移，可识别抱死趋势。
- 前轮滑移持续高于后轮，并伴随偏航响应不足，可作为转向不足的参考。
- 后轮滑移突然上升并伴随偏航率增加，可作为转向过度或甩尾的参考。

转向不足/过度属于多信号推断，不能只凭一个瞬时滑移值下结论。至少应联合车速、方向输入、偏航率、前后轮滑移和连续时间窗口。

### 5.3 悬架与车身

可以分析：

- 悬架接近最大压缩、可能触底；
- 车轮卸载、腾空、跳跃和落地；
- 制动前俯、加速后蹲和过弯侧倾；
- 左右悬架工作不对称；
- 路肩和越野路面对悬架的冲击；
- 悬架反复振荡趋势。

悬架行程对时间求导可以估算悬架运动速度，但必须处理可变帧率、时间戳异常和噪声，并使用合理滤波。没有车轮垂直载荷、减振器力和悬架几何时，不能声称完成了完整赛车工程计算。

### 5.4 动力系统

RPM、功率、扭矩、增压、挡位和车速可以用于：

- 重建当前调校的全负荷功率/扭矩曲线；
- 判断发动机有效功率区间；
- 学习各挡 RPM/车速关系和换挡转速跌落；
- 分析涡轮增压建立和换挡后的动力恢复；
- 比较不同调校的直线加速表现；
- 计算理论、实测及赛道相关的换挡建议。

### 5.5 燃油

由于 `Fuel` 只有 0 至 1 的比例，可以计算：

- 每圈消耗的油箱百分比；
- 每公里消耗比例；
- 按当前消耗估计剩余圈数或距离。

无法仅靠 Data Out 得到精确升数，因为没有油箱容量。

### 5.6 路面和事件

可以识别或辅助判断：

- 哪一侧车轮压到路肩；
- 哪个车轮位于积水中；
- 路面震动强度；
- 跳跃、腾空、落地和车辆重置造成的位置突变；
- 撞击可破坏物体的时刻、速度损失和物体质量。

`SmashableVelDiff` 和 `SmashableMass` 只明确针对可破坏物体，不能直接解释为全部撞墙、撞车或损伤事件。

## 6. 明确缺失的数据

官方明确说明 FH6 不包含 Forza Motorsport Dash 格式中的：

- `TireWear`：轮胎磨损；
- `TrackOrdinal`：赛道编号。

完整字段列表中也没有：

- 赛道、赛事或路线名称；
- 检查点和官方计时段；
- 对手车辆位置、速度、输入和轮胎状态；
- 完整排名表及车辆间隔；
- 胎压、外倾角、束角和车轮垂直载荷；
- 刹车温度、车辆损伤和空气动力数据；
- ABS、TCS、稳定辅助的介入状态；
- 天气、气温、路面温度和道路材质名称；
- 每挡齿比、终传比、车轮半径和油箱容量；
- 车辆总质量和重心位置；
- 调校唯一 ID。

因此产品应定位为“本车驾驶、圈速和调校分析”，不能承诺完整赛事直播或真实车队工程功能。

## 7. 路线模板与单圈分析

### 7.1 为什么需要保存路线模板

FH6 没有输出 `TrackOrdinal`、赛道名称或检查点。软件若要将实时位置映射为路线进度、自动识别赛道并计算实时 Delta，就需要自己的路线数据库。

路线模板应与最佳圈分离：

```text
TrackTemplate 1 ─── N LapRecord
```

- `TrackTemplate` 描述路线空间结构和识别特征。
- `LapRecord` 保存一次驾驶的时间、车辆和遥测样本。
- 更新个人最佳圈不应改变路线身份或破坏路线匹配。

### 7.2 推荐的路线模板字段

```text
TrackTemplate
  routeId
  displayName
  direction
  source                 # built_in / user_learned / imported
  gameBuild
  startFinishPoint       # x, y, z
  startFinishDirection   # 单位切向量或朝向
  boundingBox
  routeLength
  matchingTolerance
  polyline[]             # x, y, z, cumulativeDistance
  featurePoints[]
  captureLapCount
  confidence
  createdAt / updatedAt
```

一圈采集得到的是“参考路线/路线走廊”，不是完整赛道边界。遥测不包含路宽或边界，界面和文案不得声称自动还原了完整赛道模型。

### 7.3 首次路线学习流程

针对环形赛道：

1. 检测 `LapNumber` 增加，或 `CurrentLap` 明显回到起始值。
2. 将第一次完整通过起终点作为采集起点，忽略从发车格出发的残缺段。
3. 记录到下一次完整通过起终点。
4. 排除倒带、重置、明显传送、异常丢包和严重跑偏的采样段。
5. 对坐标按空间距离重采样，避免帧率影响点密度。
6. 平滑轨迹并计算累计路线距离 `s`。
7. 保存起终点位置、通过方向、路线包围盒和特征点。
8. 允许用户命名或先保存为未知路线。

一圈可以生成最低可用模板；两到三圈更适合估计正常路线走廊、过滤跑偏并稳定起终点。不要把用户进入赛事后的第一段行驶直接当成完整圈。

对于点到点冲刺赛，无法等待第二次通过起终点。可以从确认的比赛起步开始记录，并在数据流停止或比赛状态变化时结束，但必须把“暂停/菜单/完成”歧义纳入状态机。

### 7.4 自动路线识别

保存模板后可以自动识别，但有两个限制：

1. Data Out 只有开始驾驶后才发送，所以无法在菜单或刚载入赛事时立即识别。
2. 官方只说 `PositionX/Y/Z` 是世界坐标，没有保证它跨版本永久稳定；标准地图坐标能否跨重启、季节和版本复用必须实测。

建议状态机：

```text
Unknown -> Candidate -> Confirmed
             |             |
             +--> Unknown <-+
```

识别流程：

1. 使用最初位置、包围盒和起点邻域快速筛选候选路线。
2. 使用移动方向区分正向和反向路线。
3. 累积一段连续轨迹，与候选模板进行地图匹配。
4. 比较点到路线距离、方向夹角、高度误差和路线进度连续性。
5. 只有最佳候选超过阈值且显著优于第二候选时才确认。
6. 置信度下降或位置跳变时退回候选/未知状态，不强行保持错误路线。

概念性评分可以写为：

```text
score = w_pos  * positionScore
      + w_dir  * directionScore
      + w_y    * elevationScore
      + w_prog * progressContinuityScore
```

不能只比较起点坐标，因为正反向路线、共享起点的不同赛事、EventLab 蓝图和自由漫游都可能经过相同区域。立交桥和交叉路线必须利用 `Y` 高度及历史进度连续性，不能逐帧在全路线中无状态地寻找最近点。

### 7.5 路线进度与实时 Delta

把每个实时位置投影到模板折线，得到沿路线的累计进度 `s`。参考圈保存函数：

```text
referenceTime = f(s)
```

实时圈速差：

```text
delta(s) = currentLapElapsed - referenceTime(s)
```

实现时应：

- 对参考时间按路线距离插值；
- 限制投影只能在上一进度附近向前搜索；
- 为发卡弯、交叉点和起终点回绕单独处理；
- 跑出路线时冻结、降置信度或标记无效，不能跳到另一段路线；
- 将参考圈、当前圈和理论最佳分段圈区分显示。

由此可以生成速度、油门、刹车、挡位、滑移和圈速损失的路线热力图。

### 7.6 路线识别特殊情况

- 正向和反向布局应保存为两个模板。
- EventLab 可能让多个完全不同的路线共用官方起点，需要更长的轨迹确认。
- 自由漫游经过起点不能直接认定为有效比赛圈。
- 倒带期间无数据；恢复后要检查时间、位置和路线进度是否连续。
- 车辆重置或快速移动会造成坐标跳变，应分段并使当前圈失效或降低可信度。
- 模板应带游戏版本，并支持重新校准和迁移。

## 8. 最优换挡点

### 8.1 定义

理论最佳升挡点不是峰值扭矩点，也不一定是峰值功率点。它是相同车速下，当前挡位可用车轮驱动力开始低于下一挡位的位置。

设：

- 当前挡齿比为 `G_i`；
- 下一挡齿比为 `G_next`；
- 当前发动机转速为 `n`；
- 扭矩曲线为 `T(n)`。

升挡后的转速：

```text
n_after = n * G_next / G_i
```

忽略不同挡位的效率差异时，升挡条件为：

```text
T(n) * G_i <= T(n_after) * G_next
```

在相同车速下也可以用功率曲线比较：

```text
P(n) <= P(n_after)
```

如果到转速限制前都没有交点，就应在限制器前换挡。每对相邻挡位的比例可能不同，因此必须输出 `1->2`、`2->3` 等各自的换挡点，不能只保存一个全车通用 RPM。

### 8.2 学习相邻挡位比例

Data Out 不提供齿比，但不一定需要终传比和轮胎半径。稳定、低滑移、非换挡状态下，对每挡拟合：

```text
K_i = RPM / Speed
```

则相邻挡位比例近似为：

```text
G_next / G_i ~= K_next / K_i
```

也可以从真实换挡前后的 RPM 比例直接估计。采样时应过滤：

- 明显驱动轮打滑；
- 离合器操作和换挡过渡；
- 跳跃、腾空和碰撞；
- 极低车速造成的数值不稳定；
- 油门快速变化和异常丢包。

### 8.3 获得发动机曲线

如果游戏内只显示曲线图片，从截图描点只能得到近似数据。更可靠的方式是用遥测自动学习当前车辆配置的实测曲线：

1. 在平直路段选择不易打滑的中间挡位。
2. 从低 RPM 到限制器附近保持高油门。
3. 按 RPM 分桶记录 `Power`、`Torque` 和 `Boost`。
4. 多次运行取中位数并平滑。
5. 排除换挡、滑移、跳跃、碰撞和明显坡度干扰。

静态游戏曲线可以作为先验，遥测曲线用于校准动态增压、传动状态和实际游戏行为。

### 8.4 理论点、实测点和赛道点

软件最好同时维护三种建议：

- **理论换挡点**：功率/扭矩曲线与相邻挡位比例的交点。
- **实测最快点**：多次直线加速中，由真实纵向加速度或分段用时校准。
- **赛道建议点**：结合弯道、抓地、即将到来的制动区以及避免弯中换挡进行修正。

低附着路面上，理论车轮扭矩可能已经超过轮胎可传递的抓地力。软件应结合驱动轮滑移和实测加速度判断是否值得提前升挡，不能把干地理论点应用于所有场景。

### 8.5 换挡提示提前量

如果在目标 RPM 到达时才提示，玩家反应和游戏执行延迟会导致实际换挡过晚。提示点可以动态计算：

```text
cueRpm = targetRpm - rpmRiseRate * totalLatency
```

`totalLatency` 可以包括玩家平均反应时间、输入处理延迟和换挡执行时间，并通过历史换挡自动学习。

### 8.6 车辆配置身份

`CarOrdinal` 只标识车型，不标识发动机升级、发动机互换或变速箱调校；PI 也不是调校唯一 ID。换挡模型应绑定“车辆配置档案”，至少记录：

- `CarOrdinal`、`CarClass`、PI、驱动形式和气缸数；
- 最大 RPM；
- 实测功率/扭矩曲线摘要；
- 各挡 `RPM/Speed` 斜率；
- 当前可观察到的换挡转速跌落。

当以上特征明显变化时，应提示重新学习，不能继续套用旧换挡点。

## 9. 推荐的软件模块

```text
UDP Receiver
    -> Packet Validator
    -> FH6 Packet Parser
    -> Timestamp / Unit Normalizer
    -> Stream & Session State Machine
         -> Track Matcher
         -> Lap Detector
         -> Vehicle Profile Matcher
    -> Analysis
         -> Live Delta / Lap Comparison
         -> Driver Input Analysis
         -> Tire & Suspension Analysis
         -> Engine Curve & Shift Analysis
    -> Storage
    -> Dashboard / Reports / Alerts
```

模块职责建议：

- **Receiver**：socket、缓冲区、数据流超时和基础统计。
- **Validator/Parser**：包长、类型、偏移、字节序校验，不承载业务逻辑。
- **Normalizer**：统一时间、速度、角度和输入比例，保留原始值。
- **State Machine**：区分连续驾驶段、暂停/倒带后的新段、有效圈和无效圈。
- **Track Matcher**：只负责路线候选、置信度、进度和方向。
- **Lap Detector**：组合 `LapNumber`、`CurrentLap`、起终点穿越和流状态，不依赖单一信号。
- **Vehicle Profile**：识别车辆配置变化，管理功率曲线和挡位模型。
- **Analysis**：所有推导结果保留方法版本、置信度和输入数据范围。
- **Storage**：原始数据与派生数据分层，确保算法升级后可以重算。

高帧率原始遥测数据量较大。建议保留原始包或完整解码帧用于短期调试，对长期数据按圈分块、压缩或降采样，同时保留关键事件附近的高频数据。

## 10. 最低验证清单

### 协议解析

- [ ] 实测确认收到的包恒为 324 字节。
- [ ] 校验每个偏移，特别是 FH6 新增的 `CarGroup`、`SmashableVelDiff`、`SmashableMass`。
- [ ] 实测确认字节序和偏移 323 的内容。
- [ ] 验证 30/60/120/非固定帧率下的采样间隔。
- [ ] 模拟丢包、乱序、重复包和 `TimestampMS` 回绕。
- [ ] 验证暂停、菜单、回放、倒带和完赛时的数据流行为。

### 字段语义

- [ ] 对照游戏界面确认速度、功率、扭矩、增压和输入换算。
- [ ] 实测轮胎温度单位。
- [ ] 实测空挡、倒挡及特殊挡位值。
- [ ] 实测驾驶线和 AI 刹车差值的正负方向。
- [ ] 跨车辆和场景验证四轮角标顺序。

### 路线与单圈

- [ ] 验证世界坐标跨游戏重启、车辆、季节和版本是否稳定。
- [ ] 测试共享起点、正反向、发卡弯、立交桥和交叉路线。
- [ ] 测试自由漫游经过赛道时不会误识别为有效圈。
- [ ] 测试车辆重置、倒带和跑出路线后的状态恢复。
- [ ] 环形赛、点到点冲刺赛和 EventLab 分别验证。
- [ ] Delta 投影保持路线进度单调且能正确处理起终点回绕。

### 换挡分析

- [ ] 每一对挡位单独学习比例和换挡点。
- [ ] 过滤轮胎打滑、离合器和换挡过渡样本。
- [ ] 对比曲线理论值与多次直线加速实测结果。
- [ ] 验证不同手动/离合/自动模式的换挡延迟。
- [ ] 修改发动机、终传或单挡齿比后能识别旧模型失效。
- [ ] 在干地、湿地和越野低附着条件下分别验证。

## 11. Agent 实施原则

1. 优先保存原始遥测，不可只保存当前版本算法的派生结果。
2. 所有推导指标应记录算法版本、置信度和失效原因。
3. 不把 `CarOrdinal` 当作车辆调校 ID。
4. 不把玩家一圈的赛车线宣传成完整赛道边界。
5. 不承诺进入赛事瞬间识别；只能在开始驾驶并获得足够轨迹后确认。
6. 不把轮胎归一化滑移当作未经处理的现实物理滑移率/滑移角。
7. 不在实测前假定轮胎温度单位、特殊挡位映射、字节序或跨版本坐标稳定性。
8. 不把峰值功率或红线直接当作最优换挡点；必须结合下一挡转速落点。
9. 路线模板、参考圈、车辆配置和分析结果应作为独立实体管理。
10. 对暂停、倒带、重置、传送、丢包和完赛后的数据停止建立明确状态机。

## 12. 官方来源

- [Forza Horizon 6 “Data Out” Documentation](https://support.forza.net/hc/en-us/articles/51744149102611-Forza-Horizon-6-Data-Out-Documentation)

本文中的路线识别、实时 Delta、发动机曲线学习和最优换挡计算均为基于官方遥测字段的工程方案，不是官方承诺的内置功能。

## 13. 仪表盘视觉参考

- [FH6 极简遥测仪表盘 V2 透明设计图](./design/FH6_TELEMETRY_DASHBOARD_DESIGN_V2_TRANSPARENT.png)
- [V2 布局、等级配色、透明窗口与动画复刻规格](./design/FH6_TELEMETRY_DASHBOARD_SPEC.md)
- [V2 ImageGen 复现提示词与透明输出流程](./design/FH6_TELEMETRY_DASHBOARD_PROMPT_V2.md)
- [V1 设计图（历史参考）](./design/FH6_TELEMETRY_DASHBOARD_DESIGN.png)
- [V1 ImageGen 提示词（历史参考）](./design/FH6_TELEMETRY_DASHBOARD_PROMPT.md)

## 14. LazyForza 开发 Agent 提示词

- [开发提示词套装说明](./LAZYFORZA_AGENT_PROMPT_PACK.md)
- [总控实施提示词](./prompts/00_MASTER_IMPLEMENTATION_PROMPT.md)
- [阶段 1：技术选型、模块底座与遥测核心](./prompts/01_FOUNDATION_AND_TELEMETRY.md)
- [阶段 2：仪表盘与自动换挡学习](./prompts/02_DASHBOARD_AND_SHIFT_LEARNING.md)
- [阶段 3：赛道学习、分段 HUD 与逐圈分析](./prompts/03_TRACK_AND_LAP_ANALYSIS.md)
- [阶段 4：Fluent UI、整体验收与交付](./prompts/04_FLUENT_UI_INTEGRATION_AND_QA.md)
