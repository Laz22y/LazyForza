# 真实 FH6 验证清单

本文件严格区分离线完成项与真实 Forza Horizon 6 验证项。2026-07-22 已开始 Live UDP 实机核验；只勾选本次会话直接观察到的行为，其余项目继续保留。

## 地产赛事开发预览（尚未完成多人实机验证）

### 已由 Simulator/自动测试/本地服务完成

- [x] 客户端以 10 Hz 上传归一化赛道位置、圈速、分段、维修区位置和抓地趋势，不以 HUD 刷新频率发送网络数据；
- [x] 比赛密码不写入 LazyForza 数据库，断开后清除内存中的连接资料；断线使用恢复令牌自动重连；
- [x] 自动测试覆盖长时间遥测间断、时间戳倒退、2–12 人房间上限、排位/正赛排序、旗语、处罚、掉线恢复和重复圈事件；
- [x] 维修区通道由实际驶入路线确定，入口/出口由通道首尾方向生成，换胎区由 4–8 个停车边界点确定；
- [x] 维修区通道与起终点平面的交点会生成独立计时门；自动测试确认主赛道有限门覆盖不到维修区时，合法驶入维修区仍只结算一次有效圈，维修区轨迹不会被误判成偏离赛道；
- [x] 维修停留只按换胎区内不高于 5 km/h 的连续游戏时间计数；暂停时冻结并在恢复后继续，移动或离开区域会中断，同一次进入只计一次；
- [x] 1280×720、1920×1080、2560×1440 离屏截图确认排行榜、赛道一览、抓地提示和赛事横幅可读且不遮挡等级/PI；
- [x] 自动测试确认地产赛事画布拥有独立位置与尺寸；移动、缩放主仪表盘不会改变赛事四块的位置，旧布局首次读取时仍保持原来的屏幕位置；
- [x] FH6 UDP 未提供轮胎磨损、车损或换胎完成字段；所有界面均把抓地状态写为本圈滑移趋势，把维修状态写为位置/停留规则，不声称游戏已确认换胎。

### 必须用真实 FH6 多机验证

- [ ] 2 名和 3 名车手分别完成登录、准备、排位、发车倒计时、正赛、方格旗和赛后处罚全流程；
- [ ] 真实网络短暂断开后，恢复令牌能保留同一车手身份、排位成绩、正赛圈数和处罚；
- [ ] 两台以上客户端在同一地产中连续行驶，赛道一览的位置、方向、排名和维修区状态与各自主画面一致；
- [ ] 在可视化布局编辑器中把赛事四块分散到游戏窗口四周，再单独移动、缩放或隐藏主仪表盘；确认排行榜、赛道一览、抓地提示和赛事横幅的位置与大小均不受影响；
- [ ] 分别验证排位赛暂停/菜单会取消当前圈，正赛暂停/菜单会保留当前圈并在恢复后继续计圈；倒带、传送和明显回转在两种阶段都应取消当前圈；
- [ ] 正常方向逐圈冲线至少 5 圈，服务端圈数与各客户端本地有效圈严格一致，不因遥测包重复、重连或失序多计；
- [ ] 实际驶入维修通道、进入换胎区、按规则连续停车并驶出，入口/出口方向、限速提示和停留进度均正确；
- [ ] 从维修区通道通过起终点至少 3 次，确认不漏圈、不重复计圈，且驶出维修区后的下一圈分段与圈速分析连续；
- [ ] 手动切换车损设置前后记录完整 UDP 与诊断包，只验证可观察字段是否变化；在没有证据前不得把维修停留写成已换胎；
- [ ] 4–12 人继续只视为确定性多进程覆盖，除非今后具备对应数量的真实 FH6 联机环境。

## 已由 Simulator/Replay/自动测试完成

- [x] 只接收单向 UDP/回放输入，代码不包含内存读取或 DLL 注入；
- [x] 严格拒绝非 324 字节包，只解析命名偏移 0..322并保留尾字节 323；
- [x] 显式小端解析和关键字段合理性拒绝；
- [x] 30/60/120/144 Hz 输入不依赖固定包率；
- [x] Timestamp 重复、乱序、回绕、异常间隔和断流状态；
- [x] 版本化原始包录制/确定性回放和损坏文件安全失败；
- [x] Dashboard/Lap 模块幂等启停、失败隔离、订阅和 HUD 释放；
- [x] 合成曲线逐挡交点、无交点 fallback、Cue 提前量、打滑过滤，以及车辆/发动机/传动指纹变化后立即保存旧档案并切换到全新的学习状态；
- [x] 5 m 路线重采样、环道闭环、点到点独立终点、进度单调、交叉/高度受约束投影和确定性分段；
- [x] 点到点首次学习覆盖两条确定性完成路径：官方 `LastLap` 更新；以及 `LastLap` 不更新但从比赛开局连续采集、无回转、开放路线至少 300 m、起终点分离并确认回到漫游的受约束退出兜底；反例确认比赛中途开始采集不会误存；
- [x] 首圈模板、后续自动确认、有效圈保存、分段时间，以及 Gray/Yellow/Green/Purple 的双基准颜色分类；
- [x] 已完成分段颜色锁定、过线后完整旧圈按可配置时长保留（默认 2 秒）、下一圈自动切换；
- [x] 同圈计时倒退会裁掉未来样本、全局重投影并继续分析，且不会误判为冲线；
- [x] 比赛发车位不参与路线采集；首次 `CurrentLap` 清零但 `LapNumber` 不变、`CurrentRaceTime` 前进时，识别为真实首次过线；
- [x] 自动测试覆盖后续圈 `CurrentLap` 先清零、`LapNumber` 下一帧增加：冲线只触发一次、连续两圈均保存，并优先采用 `LastLap` 总圈速；
- [x] 自动测试覆盖终圈没有 `CurrentLap` 清零或 `LapNumber` 增加：若 FH6 更新 `LastLap` 则立即使用官方值保存；若计数器均未更新，则仅在确认退出比赛且路线末端、起终点距离、投影率和所有分段覆盖均通过时保守收尾；
- [x] 倒带反例测试覆盖起点附近、`LastLap` 同帧变化及随后立即退出比赛：圈计时/圈号回退不能触发终圈保存，倒带后未继续行驶至少 1 秒也不能触发退出兜底；
- [x] 自动测试覆盖菜单/暂停零帧挂起但不清空比赛 Session、本圈样本与起终点状态；恢复后同圈倒带裁剪未来样本，且中断发生在冲线前仍可连续保存两圈；
- [x] 自动测试覆盖比赛退出后的最近比赛快照：当前 Session 结束但已完成圈继续供“当前比赛”页读取 5 分钟；检测到下一场比赛后立即建立新 Session 并清除旧快照；
- [x] 冲线处理与 SQLite 持久化解耦：已完成圈先进入内存并立即开始下一圈，约 10 Hz 的降采样历史样本进入有序后台写入队列；Live 订阅缓冲提升至 1024 帧；
- [x] 起点逻辑变更使用 SectorSchemaVersion 2，旧版路线和圈保留但不再参与新比较；
- [x] 灰=未跑/无效、黄=慢于本场同等级、绿=本场同等级最快、紫=程序保留的同赛道同等级历史最快；自动测试确认 S1 不会误用更快的 S2 分段；不声称在线世界纪录；
- [x] 每赛道总计 50 圈上限、自动清理最旧非历史最快圈、每个性能等级各自最快自动保护和手动删除的存储测试；
- [x] 圈速列表只批量读取轻量 `LapSummary`；图表确认显示后才批量读取最多 4 圈采样。速度曲线使用保留峰谷的包络降采样和冻结几何，新增测试确认短暂最高速、最低速及首尾采样不会丢失；
- [x] 删除所选赛道圈速记录的存储与模块集成测试：可限定当前筛选等级，默认保留删除范围内每个等级最快有效圈，显式选择后才连同各等级最快一起删除；两种模式都只清除圈、分段和采样，赛道模板继续保留；
- [x] SQLite SchemaVersion 8、隔离/事务、车辆学习、路线布局、分段、圈和样本往返；车辆配置名称与推荐挡位开关可持久化，旧配置默认启用；旧模板迁移为环道，旧圈从车辆指纹恢复 PI 并按规范区间补齐性能等级，新圈优先使用 UDP CarClass；85 条 Playground 官方赛事模板具有不可重命名、不可删除的内置来源标记；
- [x] 自动测试确认同一 CarOrdinal/PI 下持续显著的挡位比变化或动力曲线变化都会开启新的调校档案；稳定学习达到门槛后生成不可随追加样本漂移的调校指纹；两份同车型调校可分别重命名、删除和保存推荐挡位开关；
- [x] 赛道识别 2.0 为每条路线预计算 X/Z 空间网格，最终投影继续使用 X/Y/Z 以区分立交上下层；起点区域粗筛后最多只让 12 条候选进入连续精投影，并按初始方向、120 m 早期曲率、闭环类型和长度/进度证据继续淘汰。20 条同起点路线回归确认精匹配上限为 12；另有 12 条反向路线占满初始名额时，第 13 条正确正向路线会在方向证据出现后晋级并成功锁定。诊断快照同时保留前三名的起点距离、平均距离、进度、有效率和最近淘汰原因；
- [x] 85 条官方模板自动识别安全审计保持 0 条误锁；专项自动回放语料覆盖电器街环道、机场定点、彩虹桥高差路线、鸣尾偏离中心线 30 m 的宽越野路线，以及歌利亚/传奇岛共享起点前 250 m 延迟决策；识别期间缓存的起步帧会在锁定后重放，避免丢失首圈前段；
- [x] 已锁定路线的极度偏航检测改用预计算空间网格查询；只有持续严重偏离才撤销并从当前位置重匹配。阈值按类型保守分级：越野至少 250 m/3 s/100 m 行驶、泥地 180 m/2.5 s/75 m、山道 150 m、其他至少 120 m；自动测试确认越野相对中心线偏移 200 m 不会误触发，而数公里级错误路线会重匹配且不保存中途残圈；
- [x] Overlay 扩展样式、60 Hz limiter、透明 PNG 四角 alpha 与三档离屏分辨率；
- [x] Overlay 运行窗口的原生样式固定为点击穿透、工具窗口且不激活，旧配置即使保存了解锁或关闭穿透也会被规范化；布局编辑器的 FH6 窗口候选筛选、中心线/边缘吸附和 20%–150% 等比缩放边界具有自动测试；
- [x] Dashboard 加速度跟随的阻尼/回弹、开关与强度限制；静止等待、淡入淡出、完成圈保留、无匹配确认/淡出和 Live 断流时间全部由 `OverlayLayout` 配置并有确定性状态机测试；
- [x] Dashboard 静止淡出状态机已有确定性测试：持续静止会按配置等待并淡出；速度、油门、刹车、离合、转向、挡位或手刹状态变化会唤醒。FH6 在车库中可能持续输出 `HandBrake=255`，恒定手刹值不再被当作持续活动，但手刹发生变化仍会唤醒一次；
- [x] 自动测试确认 Dashboard 分段 Delta 使用同赛道、同性能等级真实历史最快完整圈的累计分段时间；相同确定性圈通过分段后的累计差接近 0，不使用跨圈拼接的理论最优；分段累计 Delta 与完成圈 Delta 均只显示 2 秒；
- [x] 自动测试以 40,000 帧覆盖 20 分钟超长圈：完成分段时间只在跨段时计算并缓存，后续 HUD 更新不再随本圈累计采样数持续退化；
- [x] 圈速动态遥测采用每样本 27 字节版本化量化记录，Schema 8 升级到 Schema 9 时旧样本保持空值且无需回填；存储、便携备份、算法分类、旧圈兼容、CSV 导出和动态图层 PNG 渲染均有自动回归测试；
- [x] 自动测试确认无匹配证据不会结束比赛会话；确认后 Lap HUD 淡出并锁定到 `CompetitionSessionId`，暂停同一会话不会解除，下一场比赛才恢复；
- [x] 桌面 smoke：透明、置顶、点击穿透；Dashboard/Lap 可分别停用，仅 Lap 时独立弧形面板。
- [x] 发布版 30 分钟循环 Replay soak：61 次资源采样、稳态内存无单调增长、10→30 分钟 handles 640→640、threads 49→47，正常关窗退出且未强制终止；
- [x] 自动更新确定性测试覆盖：严格选择稳定版 `win-x64` 资产、外层 SHA-256、包内清单、安全解压路径、成功替换、旧托管文件清理、无关文件保留和安装失败回滚；

## 必须用真实 FH6 验证

### 协议

- [x] 本次 Live 会话接收超过 10,000 个 324 字节有效包、无解析失败；当前菜单/驾驶包率约 75–83 Hz；
- [ ] 确认不同帧率/画面模式收到的包恒为 324 字节；
- [ ] 用真实数值确认官方未明确声明的小端字节序；
- [ ] 记录偏移 323 在多车辆、多赛事中的内容，但不赋予业务含义；
- [ ] 对照游戏界面核验 `CarGroup`、`SmashableVelDiff`、`SmashableMass` 和偏移；
- [ ] 验证 30/60/120/144/非固定帧率的真实间隔与 UDP 丢包行为；
- [x] 菜单状态实测仍约 77 Hz 发出 `IsRaceOn=0`、Timestamp/车辆字段为零的帧；这些帧不参与 HUD、车辆指纹或丢包统计；
- [ ] 分别记录暂停、回放、倒带和完赛后的停包/零帧行为；

### 字段语义

- [x] 2026-07-23 以同帧游戏“热度”界面与 HUD 对照确认 `TireTemp*` 原始值采用华氏温标：HUD 原始 96/97/104°F 分别换算为 35.6/36.1/40.0°C，与游戏约 35–42°C 的四轮显示吻合；Dashboard 展示层现换算并标注 `°C`，诊断页继续保留原始值；
- [x] 当前真实 FH6 会话确认 raw Gear `0` 显示为倒挡 R、raw Gear `1` 显示为 1 挡；本次未观察到独立空挡码，诊断页继续保留 raw 值；
- [ ] 确认 `Acceleration*` 与局部 `Velocity*` 单位；
- [ ] 实车直线加速、制动和左右转向分别确认 `Acceleration.X/Z` 的车辆坐标方向；若方向与当前惯性位移相反，按实测统一修正符号；
- [ ] 确认驾驶线与 AI 刹车差值的正负方向/语义；
- [ ] 跨车辆验证 FL/FR/RL/RR 顺序；
- [ ] 核验速度、功率、扭矩、增压和输入换算。

### Overlay/Windows

- [x] 当前 FH6 菜单/暂停状态实测 Dashboard 与 Lap HUD 均隐藏，主程序仍保持运行；
- [x] 2026-07-24 同一 Live 会话实测确认官方 UDP 无法可靠区分“车库静止”和“开放世界原地停车”：两者都可能为 `IsRaceOn=1`、零比赛/圈进度、零速度、零车轮转速、`DistanceTraveled=0`，进入车库后还会沿用进入前完全相同的世界坐标（本次均为 `-2205.477/383.403/-935.440`）。因此不硬编码任一车库坐标，也不再向用户显示无法证实的 `Garage=True/False`；
- [ ] 目视确认车库内 Dashboard 在设置的静止等待和淡出时间后完全隐藏；开放世界原地停车采用相同规则且诊断页不再误报车库。离开车库并产生真实移动或驾驶输入后按设置的淡入时长恢复，切换车辆和从车库进入开放世界期间不得短暂闪现；
- [ ] FH6 无边框窗口模式下 HUD 是否不抢焦点并保持点击穿透；
- [ ] 收到 Live 遥测后从设置页进入“设置 Overlay 布局”：确认引导准确说明关闭“设置 → 抬头显示与游戏 → 失去焦点时暂停”并要求游戏处于驾驶状态；确认后 FH6 位于灰色透明编辑层后方，Dashboard、比赛圈速弧、分段颜色和累计 Delta 等全部 HUD 元素固定显示，不再受非驾驶、非比赛、遥测超时或静止淡出逻辑影响；绿色边框可拖动，四条边及四角控制点均能连续、稳定地等比缩放，四角拖动不会因横纵变化切换而跳变，保存比例不再按 1% 取整；靠近窗口边缘及水平/垂直中心时出现吸附线和文字提示；取消不改变布局，保存后立即应用且重启保留；
- [ ] 未启动 FH6、未启用 Data Out、Live 数据已中断或使用 Simulator/Replay 时点击“设置 Overlay 布局”，只显示准确提示，不切换前景窗口也不打开编辑层；
- [ ] FH6 独占全屏是否允许第三方置顶窗口；若不允许，在 README 明确要求无边框窗口；
- [ ] 100/125/150/200% DPI、多显示器混合 DPI、负坐标显示器和跨屏拖动；
- [ ] 真实 FH6 Live UDP 30 分钟 soak：Task、订阅、窗口、内存无持续增长；
- [ ] 断网、端口占用、数据库只读和磁盘写失败的人类可读错误；
- [ ] 下一次发布高于当前版本时，使用正式发行版完成一次真实多源更新：正常网络下确认优先从 GitCode 检查和下载；分别模拟 GitCode 检查失败、附件下载失败和校验失败，确认只回退到 GitHub 的同一版本。验证启动仅提示不强制、取消后可继续使用、确认后自动下载/校验/重启、隔离 `--data-dir` 原样沿用；再模拟不可写安装目录验证 UAC 提权；
- [ ] HDR/高对比度/文本缩放与减少动态。
- [ ] Live 驾驶中验证 Dashboard 加速度跟随不会抖动或遮挡，0%/默认 50%/100% 强度差异合理；新版 50% 应约等于旧版 100%，新版 100% 约为旧版两倍位移；停车且松开全部输入满默认 2 秒后约 0.8 秒淡出，轻触转向/踏板、换挡或车辆移动后约 0.8 秒恢复到设置不透明度；菜单/暂停仍立即隐藏；
- [ ] 在设置页分别修改 Dashboard 静止等待/淡入淡出、完成圈保留、无匹配确认/淡出和 Live 断流隐藏时间，实机确认每项独立生效且重启后保留；

### 路线与圈

- [ ] 在同一赛道分别用至少两个性能等级连续比赛，目视确认 HUD 绿色只代表本场同等级最快、紫色只代表程序同赛道同等级历史最快，不会跨等级比较；较慢分段为黄色、下一圈未跑分段为灰色；
- [ ] 在圈速分析选择已有传奇岛环道圈，确认走线预览与赛道页缩略图上下方向一致；在走线预览和赛道页缩略图中，环道起终点旁方向箭头均沿实际行驶方向，定点赛起点保持原圆点、终点均为圆形方格旗；走线悬停提示出现时鼠标指针隐藏，移开后恢复；
- [ ] 用已有同等级历史最快圈跑一圈，逐个分段核对 Dashboard 中央绿色负值/黄色正值等于“当前圈起点至该分段终点”减去“历史最快完整圈起点至同一分段终点”，并确认不是单分段差；
- [ ] 使用本次开发预览完整跑一次“歌利亚”：至少连续行驶 20 分钟，确认 Lap HUD 全程持续更新且不会在约 15 分钟时消失；逐段 Delta 和完成圈 Delta 均在出现 2 秒后消失，最终圈速仍正常保存；
- [ ] 在圈速分析仍预选“歌利亚”时进入“电器街环道赛”，确认起跑后约 60–150 m 内自动识别并切换到电器街、首圈前段及整圈均完整保存；若共享起点/相似路线尚不能唯一判断，则继续积累路线证据而不误选；
- [ ] 在诊断页观察赛道识别 2.0：确认候选总数先经起点粗筛压缩，精匹配不超过 12 条；前三名实时展示起点距离、平均距离、进度与有效率，反向、曲率、投影或排名淘汰时显示明确原因；
- [ ] 分别实跑歌利亚/传奇岛共享起点、彩虹桥等高差路线、鸣尾越野宽路线和机场径道赛：共享路段不得抢先锁定，高低层不得串线，鸣尾允许检查点内宽走线但不得误锁机场；
- [ ] 进入所有模板均不匹配的正式比赛，确认持续无候选达到设置时间后 Lap HUD 按设置时长淡出且本场不再出现；暂停、菜单、回转后仍保持隐藏，开始下一场比赛后解除锁定；
- [ ] 实机确认“当前比赛”页在比赛进行中显示实时分段；退出比赛后继续显示上一场已完成圈 5 分钟但不显示当前圈，满 5 分钟自动隐藏；若期间开始下一场比赛则立即切回新的实时内容；
- [ ] 实机确认 D/C/B/A/S1/S2/R/X 筛选块可多选、全不选时无圈速显示，每圈等级/PI 与游戏一致；确认每赛道总计 50 圈自动保留、单圈手动删除，以及批量删除弹窗中的“仅删除选中等级”和“同时删除各等级历史最快圈”；验证未选等级和赛道模板不受影响；
- [ ] 打开圈速分析页时确认性能等级默认只选该赛道已有圈速的等级；连续勾选 1–4 圈时图表保持不变，点击“显示勾选圈数据”后才一次刷新。使用“迎战巨汉”等长路线确认勾选、滚动、曲线悬停和走线悬停无明显卡顿或页面频闪；
- [ ] 使用新版完整记录一圈铺装环道和一圈越野路线，逐项切换油门、刹车、方向、轮胎滑移、疑似转向不足/过度、出弯空转和制动失稳图层；对照游戏输入与明显失控位置检查颜色时序，并确认这些判定始终以“疑似/证据”表达，不当作车辆物理结论；
- [ ] 在圈速分析导出至少一张动态图层 PNG，确认当前图层、图例、圈速关键信息和走线完整可读且图片尺寸受限；再打开更新前记录的旧圈，确认油门/刹车仍可显示，其余图层明确提示动态遥测不可用；
- [ ] 在回放工作台选择短圈与歌利亚等长圈，验证播放、暂停、拖动、0.5×/1×/2×/4×、移动位置标记和图层切换；导出的 CSV 应在 Excel 中正常显示中文，新圈包含方向及四轮滑移，旧圈对应字段留空；
- [ ] 世界坐标跨游戏重启、车辆、季节和版本的稳定性；
- [ ] 正反向、共享起点、发卡、交叉、立交桥和自由漫游误识别；
- [ ] 实测 FH6 倒带时 `LapNumber`/`CurrentLap` 的变化，并确认同圈倒带后可继续形成有效圈；跨越起终点倒带仍按保守策略使当前圈无效；
- [ ] 实测比赛中打开菜单/暂停后返回，确认当前 Session 与本圈采样保持；再进行一次同圈倒带，确认不会被误判为比赛结束或另开一场；
- [ ] 实测比赛发车位到起跑线期间 `CurrentLap`、`CurrentRaceTime`、`LapNumber` 的逐帧变化，确认首次同圈清零判定；
- [ ] 连续完成至少三圈，确认后续圈在 `CurrentLap` 清零和 `LapNumber` 延迟更新时仍逐圈保存；对照游戏 UI 核验保存圈速与 `LastLap`，目标误差不超过 1 ms；
- [ ] 完成一场至少三圈的真实比赛，确认终圈即使直接进入结算画面也会记录；检查日志触发来源为 `last-lap update` 或退出比赛后的 `recovered final lap`，并核对终圈总时间；
- [ ] 在同一圈分别做一次倒带后继续完赛、倒带后暂停/菜单、倒带后直接退出，确认前者仍能在真实完成后保存，后两者不会伪造终圈；
- [ ] 修复后台持久化后重新连续完成至少三圈，确认每次冲线日志中的 `liveSamples` 均保持数千级、下一圈不再因上圈写库而只剩少量投影样本；
- [ ] 重置、传送、严重跑偏和异常丢包使当前圈无效；
- [ ] 在一场越野赛中主动走检查点允许的最宽路线，确认不会触发重匹配；随后驶离赛事区域超过约 250 m 并持续行驶，确认 HUD 重新识别或安全显示未匹配；
- [ ] 环形赛事实际起终点穿越和 `LapNumber/CurrentLap` 行为；
- [ ] 完成一场真实点到点比赛，确认首次学习在 `LastLap` 更新时保存为“点到点”、模板终点不闭合回起点；再次参加同一路线时确认无需第二圈或圈数重置即可从起点连续分段，并在终点保存官方成绩；同时记录完赛前后 `LastLap`、`CurrentLap`、`LapNumber` 和停包/零帧顺序；
- [ ] EventLab 当前仍为受限/实验性，不宣称稳定支持；
- [ ] 游戏版本变化后的模板重新校准/迁移策略。

### 换挡

- [x] 当前车辆一次短程实测得到 142 个有效样本、10/10 RPM 桶、2 个稳定挡位，状态 Ready、置信度 72%；倒挡样本被排除；
- [ ] Live 状态下更换到另一辆不同 CarOrdinal/PI 的车辆，确认“车辆与换挡”页在下一批有效驾驶帧内切换车辆编号、等级与 PI，旧车样本不再显示，新车从 0 个有效样本重新学习；再换回旧车时不得继续显示另一辆车的配置；
- [ ] 为同一车型准备两套可观察特征不同的调校：先验证不同 PI 会立即分为两份配置；再尽量使用相同 PI 但齿比或动力不同的调校，确认连续有效加速后保存为两个独立档案，并能在“车辆与换挡”页分别重命名、删除和设置推荐挡位开关；
- [ ] 分别驾驶推荐挡位已启用和已关闭的配置，确认关闭配置不显示升降挡箭头、重新切回启用配置后恢复；仪表盘其他车辆数据和后台学习保持正常；
- [ ] 记录仅改变悬架、胎压等 UDP 不可观察参数且 PI、动力与齿比均未变化的调校；此类配置不要求强行拆分，界面不得声称官方提供了调校 ID；
- [ ] 每对相邻挡位的真实 `RPM/Speed` 比例和换挡后 RPM 跌落；
- [ ] 平直高油门拉转与游戏内曲线/多次直线加速对比；
- [ ] 手动、手动离合和自动模式的提示/执行延迟；
- [ ] 更换车辆、发动机、终传或单挡齿比后立即保存旧档案并切换到对应新配置，不混用旧样本；
- [ ] 干地、湿地和越野低附着下的模型置信度与提前升挡限制。

### 数据保护与诊断

- [ ] 在正式发行版的真实数据目录中分别导出“仅配置”“仅车辆”“仅圈速”“仅自定义赛道”和完整 `.lfzbackup`；使用隔离数据目录逐项导入，确认冲突预览数量准确，“合并”保留本机记录，“覆盖冲突”只替换选中类别，重启后赛道、圈速和车辆模型均可正常读取；
- [ ] 将完整 `.lfzbackup` 复制到另一台 Windows 电脑，确认清单中的应用版本、Schema 版本和 `data.json` SHA-256 校验通过；导入后官方赛事仍为只读，个人圈速、车辆名称、推荐挡位开关和自定义赛道保持一致；
- [ ] 使用下一次正式自动更新验证安装器启动前生成 `Backups\auto-update-*.lfzbackup`，连续触发超过 8 次自动备份后只轮换自动文件，手动备份不受影响；
- [ ] Live 驾驶中分别模拟 UDP 停包后恢复、时间戳异常、比赛帧内车辆配置突变、赛道严重偏离后重匹配、未结算圈和比赛结束误判后恢复；每种情况均导出 `.lfzdiag`，确认包含异常前的脱敏遥测片段、状态机事件、赛道候选评分和版本信息，不包含用户名、用户目录或原始 324 字节 UDP 数据；
- [ ] 用一次正常完整比赛确认普通结束只记录状态事件，不会误标为“比赛结束误判”；只有短时间内比赛计时继续前进时才生成 `race.false-end-recovered` 异常快照。
