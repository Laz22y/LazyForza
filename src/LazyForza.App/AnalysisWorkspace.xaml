<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="LapLibraryRow">
        <Grid Margin="4,5" ToolTip="{Binding Tooltip}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32"/><ColumnDefinition Width="105"/>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <CheckBox IsChecked="{Binding IsChosen, Mode=OneWay}" Command="{Binding SelectCommand}"
                      VerticalAlignment="Center" AutomationProperties.Name="{Binding SelectLabel}"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Time}" FontWeight="SemiBold" Foreground="{Binding TimeBrush}"/>
                <TextBlock Text="{Binding Badges}" FontSize="11" Foreground="{DynamicResource AccentBrush}" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="0,0,10,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Detail}" FontSize="11" Foreground="{DynamicResource MutedBrush}" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <Button Grid.Column="3" Content="···" Command="{Binding EditCommand}" Padding="6,3"
                    ToolTip="{Binding EditLabel}" AutomationProperties.Name="{Binding EditLabel}" VerticalAlignment="Center"/>
        </Grid>
    </DataTemplate>
    <DataTemplate x:Key="SessionLapRow">
        <Grid ToolTip="{Binding Tooltip}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/><ColumnDefinition Width="120"/><ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="64"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Number}" Foreground="{DynamicResource MutedBrush}"/>
            <TextBlock Grid.Column="1" Text="{Binding Time}" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2" Text="{Binding Delta}" Foreground="{DynamicResource MutedBrush}"/>
            <StackPanel Grid.Column="3">
                <TextBlock Text="{Binding Vehicle}" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource AccentBrush}" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <TextBlock Grid.Column="4" Text="{Binding State}" Foreground="{Binding StateBrush}"/>
        </Grid>
    </DataTemplate>
    <!-- Scoped to analysis views so other application pages retain their styling. -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,18">
                            <TabPanel IsItemsHost="True" KeyboardNavigation.TabIndex="0" />
                        </Border>
                        <ContentPresenter x:Name="PART_SelectedContentHost" Grid.Row="1" ContentSource="SelectedContent" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Padding" Value="18,12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabChrome" Background="Transparent" BorderBrush="Transparent" BorderThickness="0,0,0,2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                            <Setter TargetName="TabChrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="TabChrome" Property="Background" Value="{DynamicResource AccentFaintBrush}" /></Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="TabChrome" Property="Background" Value="{DynamicResource AccentSoftBrush}" /></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Slider">
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Background="Transparent">
                        <Border Height="4" Background="{DynamicResource BorderBrush}" CornerRadius="2" VerticalAlignment="Center" />
                        <Track x:Name="PART_Track" Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" Value="{TemplateBinding Value}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                    <RepeatButton.Template><ControlTemplate><Border Height="4" Background="{DynamicResource AccentBrush}" CornerRadius="2"/></ControlTemplate></RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Width="16" Height="24">
                                    <Thumb.Template><ControlTemplate><Ellipse Width="12" Height="12" Fill="{DynamicResource TextBrush}" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" /></ControlTemplate></Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
                                    <RepeatButton.Template><ControlTemplate><Border Background="Transparent" /></ControlTemplate></RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
